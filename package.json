{
  "name": "ocr-proxy",
  "version": "1.0.0",
  "description": "Simple image proxy for OCR (adds browser-like headers, returns data:image base64)",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "node-fetch": "^3.3.2"
  }
}